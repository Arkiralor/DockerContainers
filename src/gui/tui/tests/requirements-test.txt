# Test dependencies for Docker Container TUI

# Core testing framework
pytest>=7.4.0
pytest-mock>=3.11.1

# Coverage reporting
pytest-cov>=4.1.0

# Parallel test execution (optional but recommended)
pytest-xdist>=3.3.1

# Docker SDK (required by main application)
docker>=6.1.3

# Additional testing utilities
pytest-asyncio>=0.21.1  # For async test support
